{% extends 'base.html.twig' %}

{% block title %}Task Dashboard{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}

{% block body %}
    <div class="min-h-4/5 bg-gray-100 dark:bg-gray-700 max-w-screen-xl">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between py-4 sm:py-6 mb-4 sm:mb-8 gap-4">
                <h1 class="text-xl sm:text-2xl font-bold text-left dark:text-gray-100">Task Dashboard</h1>
                <div {{ react_component('Dashboard/AddTaskForm',{ url: path('app_task_addtask'), priorities: priority, add_task_token: csrf_token('add_task') }) }}></div>
            </div>
            <div {{ react_component('Dashboard/TaskBoard', {csfr_token : csrf_token('edit_task')}) }}></div>
        </div>
    </div>
{% endblock %}
